name: aimpark_admin
description: "AimPark internal admin panel — web only."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.10.4

dependencies:
  flutter:
    sdk: flutter

  # State management
  flutter_riverpod: ^2.6.1
  riverpod_annotation: ^2.6.1

  # Routing
  go_router: ^14.8.1

  # HTTP
  dio: ^5.8.0+1

  # Secure token storage (web uses localStorage under the hood)
  flutter_secure_storage: ^9.2.4

  # JWT decoding (no external dep needed — manual base64 decode)
  # dart:convert is available from the SDK

  # Date formatting
  intl: ^0.20.2

  cupertino_icons: ^1.0.8

  # Opening PDF document URLs in a new browser tab
  url_launcher: ^6.3.1
  fl_chart: ^1.2.0

  # Browser Blob + anchor, for handing the administrator a generated CSV.
  # The panel is web-only, so no conditional import is needed.
  web: ^1.1.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  riverpod_generator: ^2.6.5
  build_runner: ^2.4.15

flutter:
  uses-material-design: true

  # Inter is bundled rather than fetched, so the panel renders identically with
  # no network — the defence demo cannot depend on Google's font CDN.
  fonts:
    - family: Inter
      fonts:
        - asset: assets/fonts/Inter-Regular.ttf
          weight: 400
        - asset: assets/fonts/Inter-Medium.ttf
          weight: 500
        - asset: assets/fonts/Inter-SemiBold.ttf
          weight: 600
        - asset: assets/fonts/Inter-Bold.ttf
          weight: 700
